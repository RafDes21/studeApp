import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
  label: {
    fontSize: 16,
    fontWeight: "bold",
    marginBottom: 8,
  },
  input: {
    padding: 8,
    fontSize: 18,
    textAlign: "center",
    backgroundColor: "#fff",
    borderRadius: 8,
  },
});
